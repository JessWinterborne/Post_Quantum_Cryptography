{
    "name": "kms-latency-lambda",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "rm -rf dist && mkdir -p dist && esbuild src/app.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/app.cjs",
        "zip": "cd dist && zip -q -r function.zip app.cjs",
        "package": "npm run build && npm run zip",
        "deploy": "bash scripts/deploy.sh"
    },
    "dependencies": {
        "@aws-sdk/client-kms": "^3.9.0"
    },
    "devDependencies": {
        "@types/node": "^25.0.3",
        "esbuild": "^0.25.0",
        "typescript": "^5.0.0"
    }
}